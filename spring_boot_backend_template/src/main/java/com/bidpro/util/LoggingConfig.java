package com.bidpro.config;

import org.slf4j.LoggerFactory;
import org.springframework.context.annotation.Configuration;

/**
 * Logging configuration – let Spring-Boot/Logback auto-configure itself
 * via logback-spring.xml or application.yml.
 */
@Configuration
public class LoggingConfig {
    /* intentionally empty; Spring Boot handles it */
}